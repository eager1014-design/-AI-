<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🔐 관리자 로그인 - AI BUSAN PROMPT LAB</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #667eea;
            --secondary: #764ba2;
            --accent: #f093fb;
            --dark: #0a0a0f;
            --darker: #050508;
            --light: #ffffff;
        }

        body {
            font-family: 'Noto Sans KR', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .login-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 50px;
            border-radius: 30px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
            max-width: 500px;
            width: 100%;
            text-align: center;
        }

        .logo-container {
            margin-bottom: 30px;
        }

        .admin-logo {
            height: 100px;
            width: 100px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 20px;
        }

        h1 {
            font-size: 2rem;
            color: var(--darker);
            margin-bottom: 10px;
        }

        .subtitle {
            color: #64748b;
            font-size: 1rem;
            margin-bottom: 40px;
        }

        .form-group {
            margin-bottom: 25px;
            text-align: left;
        }

        label {
            display: block;
            font-weight: 600;
            color: #334155;
            margin-bottom: 10px;
            font-size: 0.95rem;
        }

        input {
            width: 100%;
            padding: 15px 20px;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
        }

        .btn-login {
            width: 100%;
            padding: 18px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 1.125rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 8px 24px rgba(102, 126, 234, 0.4);
        }

        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 32px rgba(102, 126, 234, 0.5);
        }

        .btn-login:active {
            transform: translateY(0);
        }

        .back-link {
            margin-top: 30px;
            padding-top: 30px;
            border-top: 1px solid #e2e8f0;
        }

        .back-link a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .back-link a:hover {
            color: var(--secondary);
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .login-container {
                padding: 30px 20px;
            }

            h1 {
                font-size: 1.5rem;
            }

            .admin-logo {
                height: 80px;
                width: 80px;
            }
        }

        .warning-banner {
            background: linear-gradient(135deg, #fef3c7 0%, #fde047 100%);
            border-left: 4px solid #f59e0b;
            padding: 15px;
            border-radius: 12px;
            margin-bottom: 30px;
            text-align: left;
        }

        .warning-banner strong {
            color: #92400e;
            display: block;
            margin-bottom: 5px;
        }

        .warning-banner p {
            color: #78350f;
            font-size: 0.9rem;
            line-height: 1.6;
            margin: 0;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="logo-container">
            <img src="/ai-busan-logo-round.png" alt="AI BUSAN" class="admin-logo">
            <h1>🔐 관리자 로그인</h1>
            <p class="subtitle">AI BUSAN PROMPT LAB</p>
        </div>

        <div class="warning-banner">
            <strong>⚠️ 관리자 전용 페이지</strong>
            <p>이 페이지는 관리자만 접근할 수 있습니다. 권한이 없는 경우 메인 페이지로 돌아가세요.</p>
        </div>

        <form id="adminLoginForm" onsubmit="handleAdminLogin(event)">
            <div class="form-group">
                <label for="adminId">👤 관리자 ID</label>
                <input type="text" id="adminId" placeholder="admin" required>
            </div>

            <div class="form-group">
                <label for="adminPassword">🔒 비밀번호</label>
                <input type="password" id="adminPassword" placeholder="비밀번호를 입력하세요" required>
            </div>

            <button type="submit" class="btn-login">
                🔓 로그인
            </button>
        </form>

        <div class="back-link">
            <a href="/index.html">← 메인 페이지로 돌아가기</a>
        </div>
    </div>

    <script>
        // 기본 관리자 계정 설정
        const ADMIN_CREDENTIALS = {
            id: 'admin',
            password: 'aibusan2026!', // 실제 환경에서는 암호화 필요
            name: '관리자'
        };

        // 관리자 로그인 처리
        function handleAdminLogin(event) {
            event.preventDefault();

            const adminId = document.getElementById('adminId').value.trim();
            const adminPassword = document.getElementById('adminPassword').value;

            // 입력 검증
            if (!adminId || !adminPassword) {
                alert('⚠️ 모든 항목을 입력해주세요.');
                return;
            }

            // 자격 증명 확인
            if (adminId === ADMIN_CREDENTIALS.id && adminPassword === ADMIN_CREDENTIALS.password) {
                // 관리자 토큰 생성 및 저장
                const adminToken = `admin_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
                const adminUser = JSON.stringify({
                    id: ADMIN_CREDENTIALS.id,
                    name: ADMIN_CREDENTIALS.name,
                    role: 'admin',
                    loginAt: new Date().toISOString()
                });

                localStorage.setItem('admin_token', adminToken);
                localStorage.setItem('admin_user', adminUser);

                alert(`✅ 환영합니다, ${ADMIN_CREDENTIALS.name}님!\n\n관리자 페이지로 이동합니다.`);
                window.location.href = '/admin.html';
            } else {
                alert('❌ 로그인 실패\n\nID 또는 비밀번호가 올바르지 않습니다.');
                document.getElementById('adminPassword').value = '';
                document.getElementById('adminPassword').focus();
            }
        }

        // 이미 로그인된 경우 관리자 페이지로 리다이렉트
        window.addEventListener('DOMContentLoaded', function() {
            const adminToken = localStorage.getItem('admin_token');
            const adminUser = localStorage.getItem('admin_user');

            if (adminToken && adminUser) {
                window.location.href = '/admin.html';
            }
        });
    </script>
</body>
</html>
