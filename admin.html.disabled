<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🔐 관리자 페이지 - AI BUSAN PROMPT LAB</title>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;700;900&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #667eea;
            --secondary: #764ba2;
            --accent: #f093fb;
            --dark: #0a0a0f;
            --darker: #050508;
            --light: #ffffff;
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
        }

        body {
            font-family: 'Noto Sans KR', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .admin-container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .admin-header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 30px;
            border-radius: 20px;
            margin-bottom: 30px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .admin-header h1 {
            font-size: 2rem;
            color: var(--darker);
        }

        .admin-logo {
            height: 60px;
            width: 60px;
            border-radius: 50%;
            object-fit: cover;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 20px;
            text-align: center;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.15);
        }

        .stat-icon {
            font-size: 3rem;
            margin-bottom: 15px;
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 900;
            color: var(--primary);
            margin-bottom: 10px;
        }

        .stat-label {
            font-size: 1rem;
            color: #64748b;
        }

        .users-section {
            background: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #e2e8f0;
        }

        .section-title {
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--darker);
        }

        .search-box {
            display: flex;
            gap: 10px;
        }

        .search-input {
            padding: 12px 20px;
            border: 2px solid #e2e8f0;
            border-radius: 12px;
            font-size: 1rem;
            width: 300px;
            transition: all 0.3s ease;
        }

        .search-input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
        }

        .btn {
            padding: 12px 24px;
            border: none;
            border-radius: 12px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 1rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
        }

        .btn-danger {
            background: var(--danger);
            color: white;
        }

        .btn-danger:hover {
            background: #dc2626;
            transform: translateY(-2px);
        }

        .users-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        .users-table thead {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        }

        .users-table th {
            padding: 15px;
            text-align: left;
            font-weight: 700;
            color: var(--darker);
            border-bottom: 2px solid #cbd5e1;
        }

        .users-table td {
            padding: 15px;
            border-bottom: 1px solid #e2e8f0;
        }

        .users-table tbody tr:hover {
            background: #f8fafc;
        }

        .badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 600;
        }

        .badge-vip {
            background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
            color: white;
        }

        .badge-general {
            background: linear-gradient(135deg, #60a5fa 0%, #3b82f6 100%);
            color: white;
        }

        .badge-waitlist {
            background: linear-gradient(135deg, #94a3b8 0%, #64748b 100%);
            color: white;
        }

        .badge-graduate {
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: white;
        }

        .badge-verified {
            background: var(--success);
            color: white;
        }

        .logout-btn {
            padding: 12px 24px;
            background: rgba(239, 68, 68, 0.1);
            color: var(--danger);
            border: 2px solid var(--danger);
            border-radius: 12px;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .logout-btn:hover {
            background: var(--danger);
            color: white;
        }

        @media (max-width: 768px) {
            .admin-header {
                flex-direction: column;
                gap: 20px;
                text-align: center;
            }

            .search-box {
                flex-direction: column;
            }

            .search-input {
                width: 100%;
            }

            .users-table {
                font-size: 0.85rem;
            }

            .users-table th,
            .users-table td {
                padding: 10px 5px;
            }

            .section-header {
                flex-direction: column;
                gap: 20px;
                align-items: flex-start;
            }
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #94a3b8;
        }

        .empty-state-icon {
            font-size: 4rem;
            margin-bottom: 20px;
            opacity: 0.5;
        }

        .action-buttons {
            display: flex;
            gap: 10px;
        }

        .btn-small {
            padding: 6px 12px;
            font-size: 0.85rem;
        }

        .btn-info {
            background: #3b82f6;
            color: white;
        }

        .btn-info:hover {
            background: #2563eb;
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <!-- Header -->
        <div class="admin-header">
            <div style="display: flex; align-items: center; gap: 20px;">
                <img src="/ai-busan-logo-round.png" alt="AI BUSAN" class="admin-logo">
                <div>
                    <h1>🔐 관리자 페이지</h1>
                    <p style="color: #64748b; margin-top: 5px;">AI BUSAN PROMPT LAB</p>
                </div>
            </div>
            <div style="display: flex; gap: 15px; align-items: center;">
                <a href="/index.html" class="btn btn-primary">🏠 홈으로</a>
                <button onclick="handleAdminLogout()" class="logout-btn">🚪 로그아웃</button>
            </div>
        </div>

        <!-- Stats Grid -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-icon">👥</div>
                <div class="stat-number" id="totalUsers">0</div>
                <div class="stat-label">전체 회원</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">⭐</div>
                <div class="stat-number" id="vipUsers">0</div>
                <div class="stat-label">VIP 실험체</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">✅</div>
                <div class="stat-number" id="verifiedUsers">0</div>
                <div class="stat-label">소모임 인증</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">📅</div>
                <div class="stat-number" id="todayUsers">0</div>
                <div class="stat-label">오늘 가입</div>
            </div>
        </div>

        <!-- 📊 성과지표 관리 섹션 -->
        <div class="users-section" style="margin-bottom: 30px;">
            <div class="section-header">
                <h2 class="section-title">📊 성과지표 관리</h2>
                <button onclick="refreshMetrics()" class="btn btn-primary">🔄 새로고침</button>
            </div>
            
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon">⏱️</div>
                    <div class="stat-number" id="timeReduction">75</div>
                    <div style="font-size: 2rem; font-weight: 700; color: var(--primary);">%</div>
                    <div class="stat-label">제작 시간 단축</div>
                    <div style="margin-top: 10px; font-size: 0.85rem; color: #64748b;">
                        다운로드 1회당 +0.3%
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">📈</div>
                    <div class="stat-number" id="engagementIncrease">400</div>
                    <div style="font-size: 2rem; font-weight: 700; color: var(--primary);">%</div>
                    <div class="stat-label">참여율 증가</div>
                    <div style="margin-top: 10px; font-size: 0.85rem; color: #64748b;">
                        조회 +0.5%, 다운로드 +1%, 평가 +0.8%
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">✅</div>
                    <div class="stat-number" id="consistencyRate">95</div>
                    <div style="font-size: 2rem; font-weight: 700; color: var(--primary);">%</div>
                    <div class="stat-label">일관성 달성</div>
                    <div style="margin-top: 10px; font-size: 0.85rem; color: #64748b;">
                        평가 1회당 +0.2%
                    </div>
                </div>
            </div>
            
            <!-- 활동 통계 -->
            <div class="stats-grid" style="margin-top: 20px;">
                <div class="stat-card">
                    <div class="stat-icon">👁️</div>
                    <div class="stat-number" id="totalPromptViews">0</div>
                    <div class="stat-label">프롬프트 조회</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">📥</div>
                    <div class="stat-number" id="totalPromptDownloads">0</div>
                    <div class="stat-label">프롬프트 다운로드</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">⭐</div>
                    <div class="stat-number" id="totalPromptRatings">0</div>
                    <div class="stat-label">프롬프트 평가</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">🔑</div>
                    <div class="stat-number" id="totalLoginCount">0</div>
                    <div class="stat-label">총 로그인</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">💬</div>
                    <div class="stat-number" id="kakaoJoins">0</div>
                    <div class="stat-label">카카오톡 입장</div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">🔥</div>
                    <div class="stat-number" id="activeUsers">0</div>
                    <div class="stat-label">활성 사용자 (7일)</div>
                </div>
            </div>
        </div>

        <!-- Users Section -->
        <div class="users-section">
            <div class="section-header">
                <h2 class="section-title">📋 회원 목록</h2>
                <div class="search-box">
                    <input type="text" id="searchInput" class="search-input" placeholder="이름, 이메일, 전화번호로 검색...">
                    <button onclick="searchUsers()" class="btn btn-primary">🔍 검색</button>
                    <button onclick="exportToCSV()" class="btn btn-primary">📥 CSV 내보내기</button>
                </div>
            </div>

            <div id="usersTableContainer"></div>
        </div>
    </div>

    <script>
        // 관리자 인증 확인
        function checkAdminAuth() {
            const adminToken = localStorage.getItem('admin_token');
            const adminUser = localStorage.getItem('admin_user');
            
            if (!adminToken || !adminUser) {
                alert('⚠️ 관리자 권한이 필요합니다.\n\n로그인 페이지로 이동합니다.');
                window.location.href = '/admin-login.html';
                return false;
            }
            
            return true;
        }

        // 페이지 로드 시 인증 확인
        if (!checkAdminAuth()) {
            // 인증 실패 시 스크립트 중단
        }

        // 회원 데이터 로드
        function loadUsers() {
            const allUsers = JSON.parse(localStorage.getItem('all_users') || '[]');
            return allUsers;
        }

        // 통계 업데이트
        function updateStats() {
            const users = loadUsers();
            const today = new Date().toISOString().split('T')[0];
            
            document.getElementById('totalUsers').textContent = users.length;
            document.getElementById('vipUsers').textContent = users.filter(u => u.isVIP).length;
            document.getElementById('verifiedUsers').textContent = users.filter(u => u.somoimVerified).length;
            document.getElementById('todayUsers').textContent = users.filter(u => {
                const userDate = new Date(u.createdAt).toISOString().split('T')[0];
                return userDate === today;
            }).length;
            
            // 성과지표 업데이트
            updateMetricsDisplay();
        }

        // 성과지표 표시 업데이트
        function updateMetricsDisplay() {
            const metrics = JSON.parse(localStorage.getItem('site_metrics') || '{}');
            const activities = metrics.activities || {};
            
            // 성과지표
            document.getElementById('timeReduction').textContent = Math.round(metrics.timeReduction || 75);
            document.getElementById('engagementIncrease').textContent = Math.round(metrics.engagementIncrease || 400);
            document.getElementById('consistencyRate').textContent = Math.round(metrics.consistencyRate || 95);
            
            // 활동 통계
            document.getElementById('totalPromptViews').textContent = activities.totalPromptViews || 0;
            document.getElementById('totalPromptDownloads').textContent = activities.totalPromptDownloads || 0;
            document.getElementById('totalPromptRatings').textContent = activities.totalPromptRatings || 0;
            document.getElementById('totalLoginCount').textContent = activities.totalLoginCount || 0;
            document.getElementById('kakaoJoins').textContent = activities.kakaoJoins || 0;
            document.getElementById('activeUsers').textContent = activities.activeUsers || 0;
        }

        // 성과지표 새로고침
        function refreshMetrics() {
            updateMetricsDisplay();
            alert('✅ 성과지표가 새로고침되었습니다!');
        }

        // 회원 테이블 렌더링
        function renderUsersTable(users = null) {
            if (!users) {
                users = loadUsers();
            }

            const container = document.getElementById('usersTableContainer');
            
            if (users.length === 0) {
                container.innerHTML = `
                    <div class="empty-state">
                        <div class="empty-state-icon">📭</div>
                        <h3>등록된 회원이 없습니다</h3>
                        <p style="margin-top: 10px;">첫 번째 회원이 가입하면 여기에 표시됩니다.</p>
                    </div>
                `;
                return;
            }

            let tableHTML = `
                <table class="users-table">
                    <thead>
                        <tr>
                            <th>번호</th>
                            <th>이름</th>
                            <th>이메일</th>
                            <th>전화번호</th>
                            <th>나이</th>
                            <th>등급</th>
                            <th>소모임</th>
                            <th>가입일</th>
                            <th>관리</th>
                        </tr>
                    </thead>
                    <tbody>
            `;

            users.forEach((user, index) => {
                const createdDate = new Date(user.createdAt).toLocaleDateString('ko-KR');
                
                // 회원 등급 표시
                const gradeLabel = user.gradeLabel || '일반 회원';
                const memberGrade = user.memberGrade || 'waitlist';
                let badgeClass = 'badge-general';
                
                if (memberGrade === 'graduate') {
                    badgeClass = 'badge-graduate';
                } else if (memberGrade === 'experimenter') {
                    badgeClass = 'badge-vip';
                } else if (memberGrade === 'waitlist') {
                    badgeClass = 'badge-waitlist';
                }
                
                tableHTML += `
                    <tr>
                        <td>${index + 1}</td>
                        <td><strong>${user.name}</strong></td>
                        <td>${user.email}</td>
                        <td>${user.phone}</td>
                        <td>${user.age}세</td>
                        <td><span class="badge ${badgeClass}">${gradeLabel}</span></td>
                        <td>
                            ${user.somoimVerified ? 
                                '<span class="badge badge-verified">✓ 인증완료</span>' : 
                                '<span class="badge" style="background: #94a3b8;">미인증</span>'
                            }
                        </td>
                        <td>${createdDate}</td>
                        <td>
                            <div class="action-buttons">
                                <button onclick="changeUserGrade(${user.id}, '${user.name}')" class="btn btn-primary btn-small">등급변경</button>
                                <button onclick="viewUserDetail(${user.id})" class="btn btn-info btn-small">상세</button>
                                <button onclick="deleteUser(${user.id}, '${user.name}')" class="btn btn-danger btn-small">삭제</button>
                            </div>
                        </td>
                    </tr>
                `;
            });

            tableHTML += `
                    </tbody>
                </table>
            `;

            container.innerHTML = tableHTML;
        }

        // 회원 검색
        function searchUsers() {
            const searchTerm = document.getElementById('searchInput').value.toLowerCase().trim();
            
            if (!searchTerm) {
                renderUsersTable();
                return;
            }

            const allUsers = loadUsers();
            const filteredUsers = allUsers.filter(user => 
                user.name.toLowerCase().includes(searchTerm) ||
                user.email.toLowerCase().includes(searchTerm) ||
                user.phone.includes(searchTerm)
            );

            renderUsersTable(filteredUsers);

            if (filteredUsers.length === 0) {
                document.getElementById('usersTableContainer').innerHTML = `
                    <div class="empty-state">
                        <div class="empty-state-icon">🔍</div>
                        <h3>검색 결과가 없습니다</h3>
                        <p style="margin-top: 10px;">"${searchTerm}"에 대한 결과를 찾을 수 없습니다.</p>
                    </div>
                `;
            }
        }

        // Enter 키로 검색
        document.addEventListener('DOMContentLoaded', function() {
            const searchInput = document.getElementById('searchInput');
            if (searchInput) {
                searchInput.addEventListener('keypress', function(e) {
                    if (e.key === 'Enter') {
                        searchUsers();
                    }
                });
            }

            // 초기 데이터 로드
            updateStats();
            renderUsersTable();
        });

        // 회원 등급 변경
        function changeUserGrade(userId, userName) {
            const users = loadUsers();
            const userIndex = users.findIndex(u => u.id === userId);
            
            if (userIndex === -1) {
                alert('회원 정보를 찾을 수 없습니다.');
                return;
            }
            
            const user = users[userIndex];
            const currentGrade = user.gradeLabel || '대기자';
            
            const gradeChoice = prompt(
                `👤 ${userName}님의 등급을 변경합니다.\n\n` +
                `현재 등급: ${currentGrade}\n\n` +
                `변경할 등급을 선택하세요:\n` +
                `1. 대기자 (기본 등급, 대기자용 프롬프트 제공)\n` +
                `2. 실험체 (실험체용 프롬프트 제공)\n` +
                `3. 졸업생 (졸업생용 프롬프트 제공)\n\n` +
                `번호를 입력하세요 (1, 2, 3):`,
                '1'
            );
            
            if (!gradeChoice) {
                return; // 취소
            }
            
            let newGrade = '';
            let newGradeLabel = '';
            let isVIP = false;
            
            switch(gradeChoice.trim()) {
                case '1':
                    newGrade = 'waitlist';
                    newGradeLabel = '대기자';
                    isVIP = false;
                    break;
                case '2':
                    newGrade = 'experimenter';
                    newGradeLabel = '실험체';
                    isVIP = true;
                    break;
                case '3':
                    newGrade = 'graduate';
                    newGradeLabel = '졸업생';
                    isVIP = true;
                    break;
                default:
                    alert('⚠️ 잘못된 입력입니다. 1, 2, 3 중 하나를 입력하세요.');
                    return;
            }
            
            // 등급 업데이트
            users[userIndex].memberGrade = newGrade;
            users[userIndex].gradeLabel = newGradeLabel;
            users[userIndex].isVIP = isVIP;
            
            // 저장
            localStorage.setItem('all_users', JSON.stringify(users));
            
            // 현재 로그인된 사용자가 변경된 경우 user_info도 업데이트
            const currentUserInfo = localStorage.getItem('user_info');
            if (currentUserInfo) {
                const currentUser = JSON.parse(currentUserInfo);
                if (currentUser.id === userId) {
                    localStorage.setItem('user_info', JSON.stringify(users[userIndex]));
                }
            }
            
            alert(`✅ ${userName}님의 등급이 "${newGradeLabel}"(으)로 변경되었습니다!`);
            updateStats();
            renderUsersTable();
        }

        // 회원 상세 정보 보기
        function viewUserDetail(userId) {
            const users = loadUsers();
            const user = users.find(u => u.id === userId);
            
            if (!user) {
                alert('회원 정보를 찾을 수 없습니다.');
                return;
            }
            
            const gradeLabel = user.gradeLabel || '일반 회원';

            const detail = `
📋 회원 상세 정보

━━━━━━━━━━━━━━━━━━━━
👤 기본 정보
━━━━━━━━━━━━━━━━━━━━
이름: ${user.name}
이메일: ${user.email}
전화번호: ${user.phone}
나이: ${user.age}세

━━━━━━━━━━━━━━━━━━━━
🎫 회원 등급
━━━━━━━━━━━━━━━━━━━━
회원 등급: ${gradeLabel}
전체 접근: ${user.hasFullAccess ? '✅ 가능' : '❌ 불가'}

━━━━━━━━━━━━━━━━━━━━
✅ 인증 정보
━━━━━━━━━━━━━━━━━━━━
소모임 인증: ${user.somoimVerified ? '✅ 완료' : '❌ 미완료'}
소모임 ID: ${user.somoimId || '없음'}

━━━━━━━━━━━━━━━━━━━━
📅 가입 정보
━━━━━━━━━━━━━━━━━━━━
가입일: ${new Date(user.createdAt).toLocaleString('ko-KR')}
회원 ID: ${user.id}
            `;

            alert(detail);
        }

        // 회원 삭제
        function deleteUser(userId, userName) {
            if (!confirm(`⚠️ 정말로 "${userName}" 회원을 삭제하시겠습니까?\n\n이 작업은 되돌릴 수 없습니다.`)) {
                return;
            }

            let allUsers = loadUsers();
            allUsers = allUsers.filter(u => u.id !== userId);
            localStorage.setItem('all_users', JSON.stringify(allUsers));

            alert(`✅ "${userName}" 회원이 삭제되었습니다.`);
            updateStats();
            renderUsersTable();
        }

        // CSV 내보내기
        function exportToCSV() {
            const users = loadUsers();
            
            if (users.length === 0) {
                alert('⚠️ 내보낼 회원 데이터가 없습니다.');
                return;
            }

            let csv = 'ID,이름,이메일,전화번호,나이,회원등급,소모임인증,가입일\n';
            
            users.forEach(user => {
                const memberType = user.isVIP ? 'VIP 실험체' : '일반 회원';
                const verified = user.somoimVerified ? '인증완료' : '미인증';
                const date = new Date(user.createdAt).toLocaleString('ko-KR');
                
                csv += `${user.id},"${user.name}","${user.email}","${user.phone}",${user.age},"${memberType}","${verified}","${date}"\n`;
            });

            const blob = new Blob(['\uFEFF' + csv], { type: 'text/csv;charset=utf-8;' });
            const link = document.createElement('a');
            const url = URL.createObjectURL(blob);
            
            link.setAttribute('href', url);
            link.setAttribute('download', `회원목록_${new Date().toISOString().split('T')[0]}.csv`);
            link.style.visibility = 'hidden';
            
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);

            alert('✅ CSV 파일이 다운로드되었습니다.');
        }

        // 관리자 로그아웃
        function handleAdminLogout() {
            if (confirm('🔒 관리자 로그아웃 하시겠습니까?')) {
                localStorage.removeItem('admin_token');
                localStorage.removeItem('admin_user');
                alert('✅ 로그아웃되었습니다.');
                window.location.href = '/admin-login.html';
            }
        }
    </script>
</body>
</html>
